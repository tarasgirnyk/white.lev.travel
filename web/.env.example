# --- Payload / Next ---
# Секрет для підпису токенів (згенеруй свій: openssl rand -hex 32)
PAYLOAD_SECRET=change-me-please-32-chars-min
# Публічний базовий URL сайту
NEXT_PUBLIC_SERVER_URL=http://localhost:3000
# Дозволені домени для CORS/CSRF (через кому)
PAYLOAD_PUBLIC_SERVER_URL=http://localhost:3000

# --- База даних (PostgreSQL) ---
DATABASE_URI=postgres://wlt:wlt@localhost:5432/whitelevtravel

# --- Сідинг демо-даними при першому старті ---
SEED_ON_INIT=true

# --- Дані першого адміністратора (використовуються onInit-сідом) ---
ADMIN_EMAIL=gor@miotex.com
ADMIN_PASSWORD=change-me-strong-password
